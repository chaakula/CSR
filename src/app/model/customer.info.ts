import { UserRegistration, UserLoanInformation } from './index';
export class CustomerInfo {
 
  public userRegistration = new UserRegistration();
  public userLoanInformation = new UserLoanInformation();

  constructor(
  ) {   }
}